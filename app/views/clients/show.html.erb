<p id="notice"><%= notice %></p>

<%= javascript_include_tag "http://www.google.com/jsapi", "chartkick" %>
<%#= line_chart @client.sessions.last.exercises.map{|exercise|
    {:name => exercise.name, :data => exercise.exercise_executions.group_by_day(:updated_at).count }

} %>
<%= line_chart @client.sessions.last.exercises.map{|exercise|
    {:name => exercise.name, :data => exercise.exercise_executions.group_by_day(:updated_at).count }

} %>
<%#= @client.sessions.last.exercises.count %>
<p>
  <strong>Gym:</strong>
  <%= @client.gym_id %>
</p>

<p>
  <strong>First name:</strong>
  <%= @client.first_name %>
</p>

<p>
  <strong>Last name:</strong>
  <%= @client.last_name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @client.email %>
</p>

<p>
  <strong>Machine code:</strong>
  <%= @client.machine_code %>
</p>

<%= link_to 'Edit', edit_client_path(@client) %> |
<%= link_to 'Back', clients_path %>
